package vndb_integration

// hosts
const (
	rootHost = "vndb.org"
	apiHost  = "api." + rootHost
)

// paths
const (
	itemPath = "/"
	kanaPath = "/kana"
	vnPath   = kanaPath + "/vn"
)

// params
const (
	TagsName = "tags.name"
	Rating   = "rating"
)
